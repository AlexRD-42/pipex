{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Executable (LLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/pipex",
      "cwd": "${workspaceFolder}",
	  "console": "integratedTerminal",
      "args": ["ls -l"],
      "env": { "LSAN_OPTIONS": "detect_leaks=0" },
      "stopOnEntry": false,
      "preLaunchTask": "build"
    }
  ]
}